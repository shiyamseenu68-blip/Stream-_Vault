{
  "buildCommand": "pnpm install && pnpm run build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "outputDirectory": "artifacts/streamvault/dist/public",
  "devCommand": "pnpm --filter @workspace/streamvault run dev",
  "buildEnv": {
    "PORT": "5000",
    "BASE_PATH": "/"
  }
}
